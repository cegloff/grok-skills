This is a tiny example web application (FastAPI + HTMX) intended to demonstrate the `grok-live-web-debug` skill.

It is deliberately minimal so you can focus on learning the debugging workflow rather than the application itself.

Run it with:
    python app.py

Then use the live debug controller to connect and interact with it.